events {
    worker_connections   1000;
}
http {
    server {
        # listens the requests coming on port
        listen 80;
        access_log  off;
        # / means all the requests have to be forwarded to api service
        location / {
        # resolves the IP of api using Docker internal DNS
        proxy_pass http://api:8082;
        }
    }
}